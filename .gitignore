# Data (large geospatial files - Don't commit, reproduced via pipeline, not stored.
data/raw/

# data/processed/ IS tracked — the Streamlit app reads its small CSV/JSON/PNG exports at
# runtime and needs them available without an Earth Engine call on every page load.

#Python
__pycache__/
.pyc
.venv/
.ipynb_checkpoints/

#Environment credentials
.env

#CLAUDE
CLAUDE.md
